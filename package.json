
{
  "name": "mini-ledger-platform",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}